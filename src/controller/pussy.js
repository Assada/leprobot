"use strict";

const request = require('request');
const randomPussy = require('random-vagina');
const BaseController = cb_require('controller/base');

class PussyController extends BaseController {
    process(msg) {

    }
}